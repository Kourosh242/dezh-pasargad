# دژ پاسارگاد — نسخهٔ 1.0.1

> متن آماده برای بدنهٔ GitHub Release با تگ `1.0.1` (نام نمایشی: DEZH-PASARGAD-1.0.1).

🏰 **دژ پاسارگاد** — گاوصندوق رمز عبور آفلاین، فارسی‌محور و متن‌باز برای اندروید. رمزنگاری واقعی روی دستگاه، بدون تبلیغ، بدون ردیابی و بدون ابر.

**نسخهٔ 1.0.1** (versionCode 2) — پولیش UI/UX، سلامت رمزها، و رفع باگ بحرانی نصب درون‌اپی.

---

## ✨ چه چیزی در 1.0.1 جدید است؟

### 🛠️ رفع باگ بحرانی
- **نصب بروزرسانی از داخل اپ** دیگر گیر نمی‌کند: مسیر `PackageInstaller` تا پنجرهٔ رسمی تأیید اندروید و تکمیل نصب درست کار می‌کند.
- پیام خطای بررسی بروزرسانی وقتی فایل APK در Release نیست، علت واقعی را نشان می‌دهد.

### 🎨 تجربهٔ کاربری
- کارت **سلامت رمزها** بالای صندوقچه + نشانگر قدرت روی هر ردیف.
- بازطراحی صفحهٔ **جزئیات رکورد** (کارت‌محور).
- **مولّد رمز** تمیزتر: اسلایدر پیوسته و کنترل‌های کلاس نویسه در یک کارت.
- دکمهٔ **گشودن** تمام‌عرض در صفحهٔ قفل.
- ناوبری پایین استاندارد Material 3 (بدون آرتیفکت بصری).
- کنتراست و پالت برند هماهنگ‌تر در تم روشن.

### 🔐 امنیت (بدون تغییر پروتکل)
- همان AES-256-GCM + PBKDF2-HMAC-SHA256 (۶۰۰k) + DEK فقط در RAM + Android Keystore.
- اعتبارسنجی package / versionCode / **امضا** قبل از نصب هر APK بروزرسانی.

---

## 📱 سازگاری

| مورد | مقدار |
|---|---|
| حداقل نسخهٔ اندروید | **اندروید ۱۰ (API 29)** |
| پشتیبانی کامل | **اندروید ۱۰ تا اندروید ۱۶** (و جدیدتر؛ targetSdk/compileSdk 37) |
| معماری CPU | **یونیورسال** — بدون کد بومی؛ arm64-v8a، armeabi-v7a، x86_64، x86 |
| دستگاه‌ها | گوشی و تبلت (رابط واکنش‌گرا) |
| زبان‌ها | فارسی (پیش‌فرض، RTL) و انگلیسی |

## 📦 فایل این انتشار

- `dezh-pasargad-release.apk` — APK یونیورسال بهینه‌شده با R8 (حدود **۴٫۰ مگابایت**)، امضاشده با کلید انتشار پروژه.

## 🛠 نصب / ارتقا

### نصب تازه
1. فایل APK را از همین صفحه دانلود کنید.
2. اجازهٔ «نصب برنامه‌های ناشناس» را برای مرورگر یا فایل‌منجر بدهید.
3. نصب کنید و با ساخت «رمز اصلی»، گاوصندوق خود را بسازید.

### ارتقا از 1.0.0
- از داخل اپ: **منو ← بررسی بروزرسانی** (یا تنظیمات) — دانلود، اعتبارسنجی امضا، سپس پنجرهٔ رسمی تأیید اندروید.
- یا APK همین صفحه را به‌صورت دستی نصب کنید (امضای یکسان = ارتقای درجا بدون از دست رفتن داده).

> ⚠️ APK را فقط از صفحهٔ Releases رسمی مخزن دانلود کنید. در صورت عدم تطابق امضا، نصب متوقف می‌شود.

## 🔒 یادآوری مهم

رمز اصلی شما **قابل بازیابی نیست**. از «پشتیبان‌گیری رمزگذاری‌شده» در تنظیمات استفاده کنید و عبارت عبور پشتیبان را جدا نگه دارید.

## 📄 پروانه

کد پروژه تحت پروانهٔ MIT و فونت وزیرمتن تحت SIL Open Font License 1.1 است.

---

گزارش مشکل و پیشنهادها: [Issues](https://github.com/Kourosh242/dezh-pasargad/issues)  
مخزن: [Kourosh242/dezh-pasargad](https://github.com/Kourosh242/dezh-pasargad)  
تاریخچهٔ کامل: [CHANGELOG.md](https://github.com/Kourosh242/dezh-pasargad/blob/main/CHANGELOG.md)

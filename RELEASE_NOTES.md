# دژ پاسارگاد — نسخهٔ 1.0.2

> گاوصندوق رمز عبور آفلاین، فارسی‌محور و متن‌باز برای اندروید — رمزنگاری واقعی روی دستگاه، بدون تبلیغ، بدون ردیابی، بدون ابر.

🏰 **دژ پاسارگاد 1.0.2** (versionCode 3) — نسخهٔ «حسِ زنده»: موج موشن و نظم هندسی Material 3، رمزساز سریع داخل فرم، و یک دور کامل شکار باگ. بدون تغییر در قالب داده؛ ارتقای مستقیم و درجا از 1.0.1 و 1.0.0 (همان کلید امضا).

---

## ✨ چه چیزهایی در 1.0.2 جدید است؟

### 🎬 تجربهٔ کاربری و رابط
- **ترنزیشن ناوبری:** حرکت عمودی آرام + محو شدن میان همهٔ صفحات، از جمله بازگشت پیش‌بین (predictive back) — راست‌به‌چپ‌مدات و بدون پرش.
- **رمزساز سریع در صفحهٔ افزودن/ویرایش:** کلید کنار فیلد رمز، یک شیت پایین‌جمع‌شو باز می‌کند؛ طول، دسته‌های نویسه و حذف نویسه‌های گیج‌کننده را همان‌جا تنظیم کنید و با یک لمس «استفاده از این رمز» بپذیرید.
- **کپی یک‌لمسی نام کاربری/ایمیل** در جزئیات رکورد — هر فیلد دکمهٔ کپی خودش را دارد.
- **تیک برند برای «کپی شد»:** علامت تیک با انیمیشن فنری برند کنار پیام کپی (رمزساز + جزئیات رکورد).
- **شمارندهٔ زندهٔ علاقه‌مندی‌ها** روی چیپ فیلتر — با هر حذف/افزودن همان لحظه به‌روز می‌شود.
- **شکل‌های Expressive:** گردی کارت‌ها به مقیاس مدرن Material 3 ارتقا یافت (۸/۱۲/۱۶/۲۰/۲۸dp) و همهٔ فاصله‌ها روی ریتم 4dp نشستند.

### 🛠️ رفع شد
- **بحرانی — گرفتارشدن در صفحهٔ جست‌وجو:** نوار جست‌وجوی همیشه‌فعال کلید بازگشت سامانه را می‌بلعید؛ اکنون بازگشت سامانه و جمع‌شدن نوار هر دو از صفحه خارج می‌شوند.
- **کپی نام کاربری/ایمیل از چتر محافظت بیرون بود:** اکنون همهٔ کپی‌ها زیر یک پنجرهٔ واحدِ پاک‌سازی خودکار کلیپ‌بورد هستند (پاک‌سازی به‌موقع).
- **شکستگی مونوگرام روی ایموجی:** عنوان شروع‌شده با ایموجی، خوشهٔ کامل نویسه را سالم نمایش می‌دهد.
- **رگبار تولید رمز حین کشیدن اسلایدر:** تولید فقط یک‌بار هنگام رهاکردن انگشت انجام می‌شود.
- **کلید «پویانمایی»** حالا روی ترنزیشن‌های ناوبری هم حاکم است.

### 🔐 امنیت (بدون تغییر پروتکل)
- همان مدل رمزنگاری: AES-256-GCM + PBKDF2-HMAC-SHA256 (۶۰۰٬۰۰۰ تکرار) + کلید نشست فقط در RAM + متادیتای امنیتی در Android Keystore.
- اعتبارسنجی package / versionCode / **امضا** پیش از نصب هر APK بروزرسانی؛ بدون هیچ تعاملی با Google Play.

### ✅ کیفیت
- ۲۸۹ تست واحد سبز · detekt بدون یافته · lint پاک.
- بازبینی چشمی ۱۰ صفحه در هر دو تم روشن/تیره پیش از انتشار.

---

## 📱 سازگاری

| مورد | مقدار |
|---|---|
| حداقل نسخهٔ اندروید | **اندروید ۱۰ (API 29)** |
| پشتیبانی کامل | **اندروید ۱۰ تا اندروید ۱۶** (و جدیدتر؛ targetSdk/compileSdk 37) |
| معماری CPU | **یونیورسال (Universal)** — بدون کد بومی؛ سازگار با همهٔ معماری‌ها: arm64-v8a، armeabi-v7a، x86_64، x86 |
| دستگاه‌ها | گوشی و تبلت (رابط واکنش‌گرا بر پایهٔ WindowSizeClass) |
| زبان‌ها | فارسی (پیش‌فرض، RTL) و انگلیسی |

## 📦 فایل این انتشار

- `dezh-pasargad-release.apk` — APK **یونیورسال** بهینه‌شده با R8 (حدود **۴٫۰ مگابایت**)، امضاشده با همان کلید انتشار نسخه‌های قبلی → نصب به‌صورت ارتقای درجا، بدون از دست رفتن داده.

## 🛠 نصب / ارتقا

### نصب تازه
1. فایل APK را از همین صفحه دانلود کنید.
2. اجازهٔ «نصب برنامه‌های ناشناس» را برای مرورگر یا فایل‌منجر بدهید.
3. نصب کنید و با ساخت «رمز اصلی»، گاوصندوق خود را بسازید.

### ارتقا از 1.0.0 / 1.0.1
- از داخل اپ: **منو ← بررسی بروزرسانی** — دانلود، اعتبارسنجی امضا، سپس پنجرهٔ رسمی تأیید اندروید.
- یا APK همین صفحه را به‌صورت دستی نصب کنید (امضای یکسان = ارتقای درجا).

> ⚠️ APK را فقط از صفحهٔ Releases رسمی مخزن دانلود کنید. اپ پیش از نصب، امضای دیجیتال APK را با امضای نسخهٔ نصب‌شده تطبیق می‌دهد؛ در صورت عدم تطابق، نصب متوقف می‌شود.

## 🔒 یادآوری مهم

رمز اصلی شما **قابل بازیابی نیست**؛ هیچ مسیر «فراموشی رمز» وجود ندارد. از «پشتیبان‌گیری رمزگذاری‌شده» در تنظیمات استفاده کنید و عبارت عبور پشتیبان را جدا از دستگاه نگه دارید.

## 📄 پروانه و سورس

- کد پروژه تحت پروانهٔ **MIT** و فونت وزیرمتن تحت **SIL Open Font License 1.1**.
- سورس کامل برای مطالعه و بازبینی در مخزن منتشر شده است: [Kourosh242/dezh-pasargad](https://github.com/Kourosh242/dezh-pasargad)
- تاریخچهٔ کامل تغییرات: [CHANGELOG.md](https://github.com/Kourosh242/dezh-pasargad/blob/main/CHANGELOG.md)

---

گزارش مشکل و پیشنهادها: [Issues](https://github.com/Kourosh242/dezh-pasargad/issues)

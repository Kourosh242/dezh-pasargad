# تاریخچهٔ تغییرات — دژ پاسارگاد

فرمت این سند بر پایهٔ [Keep a Changelog](https://keepachangelog.com/fa/1.1.0/) و نسخه‌بندی آن مطابق [SemVer](https://semver.org/lang/fa/) است.

## [1.0.1] — 2026-09-19

نسخهٔ پایدار دوم (versionCode **2**). تمرکز روی پولیش UI/UX موبایل‌محور، سلامت رمزها، و رفع باگ بحرانی نصب درون‌اپی.

### افزوده‌ها
- کارت «سلامت رمزها» بالای صندوقچه با نسبت قوی/متوسط/ضعیف و نشانگر سلامت روی هر ردیف (بدون نمایش عدد آنتروپی).
- بازطراحی کارت‌محور صفحهٔ جزئیات رکورد (هدر، حساب، رمز، یادداشت).
- بهبود صفحهٔ مولّد رمز: اسلایدر پیوسته، استپر +/− و سوییچ کلاس نویسه‌ها در یک کارت واحد.
- دکمهٔ «گشودن» تمام‌عرض در زون شست در صفحهٔ قفل.
- کنتراست بهتر کارت‌های تنظیمات در تم روشن.

### اصلاح‌ها
- **بحرانی — نصب بروزرسانی:** فیلتر گیرندهٔ وضعیت `PackageInstaller` به‌اشتباه data scheme الزام می‌کرد و برادکست سیستم بدون data هرگز match نمی‌شد؛ پنجرهٔ رسمی تأیید اندروید باز نمی‌شد. اکنون فیلتر بدون scheme و با `RECEIVER_NOT_EXPORTED` ثبت می‌شود.
- پیام خطای «بررسی بروزرسانی» وقتی Release فایل APK ندارد، علت واقعی را نشان می‌دهد (دیگر پیام گمراه‌کنندهٔ «اتصال اینترنت» نیست).
- حذف پیل سفارشی شناور ناوبری پایین و بازگشت به `NavigationBar` استاندارد Material 3 (حذف لکهٔ بصری).
- هماهنگ‌سازی پالت بصری: جستجو روی سطح خنثی گرم، FAB طلایی برند، مونوگرام حرف با پالت گرم.

### امنیت
- همان مدل رمزنگاری 1.0.0 (AES-256-GCM + PBKDF2 600k + Keystore meta) بدون تغییر پروتکل.
- مسیر نصب درون‌اپی همچنان package / versionCode / امضا را پیش از commit اعتبارسنجی می‌کند.

### سازگاری و بسته
- پشتیبانی از **اندروید ۱۰ تا اندروید ۱۶** (minSdk 29، targetSdk/compileSdk 37).
- APK **یونیورسال** بدون کد native — سازگار با arm64-v8a، armeabi-v7a، x86_64، x86.
- اندازهٔ APK انتشار حدود **۴٫۰ MB** (R8).

### کیفیت
- پوشش تست واحد/Robolectric گسترش‌یافته (شامل رگرسیون فیلتر PackageInstaller و مسیرهای UI).
- `detekt` و `lint` سبز.

## [1.0.0] — 2026-09-18

نخستین انتشار عمومی پایدار.

### افزوده‌ها
- گاوصندوق کامل: ساخت، ویرایش، حذف، مشاهدهٔ رکوردها با عنوان/نام کاربری/ایمیل/رمز/یادداشت، دسته‌بندی، علاقه‌مندی، جستجوی زنده و مرتب‌سازی.
- تقویم جلالی برای تاریخ ساخت و ویرایش رکوردها.
- تولیدکنندهٔ رمز عبور با `SecureRandom`، قیدهای طول/کلاس/حداقل و سنجهٔ قدرت entropy-محور (هم‌راستا با NIST SP 800-63B).
- پشتیبان‌گیری و بازیابی رمزنگاری‌شده از طریق SAF با قالب نسخه‌دار، پیش‌نمایش تغییرات و حالت‌های ادغام/جایگزینی.
- تنظیمات کامل: تم روشن/تیره/سیستم، ۵ رنگ اکسنت، مقیاس فونت، صفحهٔ شروع، دستهٔ پیش‌فرض، مهلت کلیپ‌بورد، مهلت قفل خودکار، تأییدیه‌ها و بازنشانی تنظیمات.
- بررسی بروزرسانی از GitHub Releases و نصب درون‌اپی با `PackageInstaller` رسمی اندروید به‌همراه اعتبارسنجی بسته، versionCode و امضا.
- رابط کاربری فارسی و RTL کامل به‌همراه زبان انگلیسی، فونت وزیرمتن و ناوبری پایین سه‌بخشی.

### امنیت
- رمزنگاری هر رکورد با AES-256-GCM و کلید دادهٔ تصادفی (DEK)؛ مشتق‌سازی کلید از رمز اصلی با PBKDF2-HMAC-SHA256 (۶۰۰٬۰۰۰ تکرار).
- نگه‌داری کلید نشست فقط در RAM و صفرکردن آن هنگام قفل (fail-closed).
- محافظت از پارامترهای KDF و شمارندهٔ تلاش‌ها با کلید غیرخروجی Android Keystore.
- تأخیر نمایی ورود ناموفق (تا ۱۵ ثانیه) و قفل خودکار بدون گزینهٔ «هرگز».
- پاک‌سازی خودکار کلیپ‌بورد پس از مهلت قابل تنظیم؛ مسدودسازی پیش‌نمایش/اسکرین‌شات در صفحه‌های حساس.
- عدم ذخیرهٔ هرگونه متن خام در دیتابیس؛ غیرفعال‌بودن پشتیبان‌گیری و انتقال سیستمی دادهٔ اپ.

### عملکرد
- بیلد انتشار بهینه‌شده با R8 و APK یونیورسال سازگار با همهٔ معماری‌های CPU.
- پشتیبانی از اندروید ۱۰ (API 29) تا اندروید ۱۶ و جدیدتر.

### کیفیت
- مجموعهٔ تست واحد/Robolectric به‌همراه تست دود سرتاسری و تست دستگاه.
- تحلیل ایستای detekt و lint.

[1.0.1]: https://github.com/Kourosh242/dezh-pasargad/releases/tag/1.0.1
[1.0.0]: https://github.com/Kourosh242/dezh-pasargad/releases/tag/1.0.0
